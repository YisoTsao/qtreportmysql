json.extract! handy, :id, :item, :pccitem, :receivedate, :startdate, :finishdate, :solution, :schedule, :capacity, :qual, :read, :write, :owner, :prodname, :desc, :prodpn, :fwvversion, :ftype, :pcbvers, :testres, :expl, :rpname, :rpdate, :approvename, :explain, :remark, :created_at, :updated_at
json.url handy_url(handy, format: :json)
